export const selectContacts = state => state.contacts.contacts; //получаем необходимую часть состояния

export const selectFilteredData = state => state.filter.value; //получаем необходимую часть состояния
